# Experiment of building nvc VHDL compiler for WASM

Experiment has failed. It is impossible to build nvc for WSL without massive changes because NVC uses dynamic linking and wasm does not support so.
